# CSD Solo GPU Miner — Python dependencies
# Install with: pip install -r requirements.txt

# Async HTTP client (node RPC calls)
aiohttp>=3.9.0

# YAML config
PyYAML>=6.0

# Numerical computing (CPU fallback scoring)
numpy>=1.24.0

# GPU support — install ONE of the following matching your CUDA version:
# pip install cupy-cuda12x     (CUDA 12.x)
# pip install cupy-cuda11x     (CUDA 11.x)
# pip install pycuda            (alternative CUDA backend)
#
# setup.sh installs the correct package automatically.
# Leave the lines below commented out if you let setup.sh handle it.
# cupy-cuda12x
